---
import { site, services, cities, contentPages, PENDING } from "@lib/site";
import { publishedCounties, countyPath } from "../data/counties";
const year = new Date().getFullYear();
---

<footer class="site-footer">
  <div class="wrap cols">
    <div>
      <p class="fbrand">{site.name}</p>
      <p class="muted">{site.tagline}</p>
      <p>
        <a class="fcall" href={site.phone.href}>{site.phone.display}</a>
      </p>
    </div>

    <div>
      <h2>Services</h2>
      <ul>
        {
          services.map((s) => (
            <li>
              <a href={`/${s.slug}/`}>{s.name}</a>
            </li>
          ))
        }
      </ul>
    </div>

    <div>
      <h2>Service Area</h2>
      {
        PENDING.cities ? (
          <p class="muted">Mobile service across Orange County, California.</p>
        ) : (
          <ul>
            {cities.map((c) => (
              <li>
                <a href={c.href}>{c.name}</a>
              </li>
            ))}
            {publishedCounties.map((county) => (
              <li>
                <a href={countyPath(county.slug)}>All of {county.name}</a>
              </li>
            ))}
          </ul>
        )
      }
    </div>

    <div>
      <h2>Company</h2>
      <ul>
        {
          contentPages.map((page) => (
            <li>
              <a href={page.href}>{page.label}</a>
            </li>
          ))
        }
      </ul>
      {/* Careers link intentionally omitted — add only once the client confirms
          they are hiring techs. */}
    </div>
  </div>

  <div class="wrap legal">
    <p>&copy; {year} {site.name}. Orange County, California.</p>
  </div>
</footer>

<style>
  .site-footer {
    background: var(--color-ink);
    color: var(--color-on-dark-soft);
    padding-block: 3rem 1.5rem;
    margin-top: 2rem;
  }

  .cols {
    display: grid;
    gap: 2rem;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  }

  .site-footer h2 {
    font-size: 0.82rem;
    text-transform: uppercase;
    letter-spacing: 0.09em;
    color: var(--color-glass);
    margin-bottom: 0.75rem;
  }

  .fbrand {
    font-weight: 800;
    font-size: 1.1rem;
    color: var(--color-on-dark);
    margin-bottom: 0.35rem;
  }

  .muted {
    color: var(--color-on-dark-soft);
    font-size: 0.92rem;
  }

  .site-footer ul {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    gap: 0.5rem;
  }

  .site-footer a {
    color: var(--color-on-dark-soft);
    text-decoration: none;
    font-size: 0.95rem;
  }

  .site-footer a:hover {
    color: var(--color-on-dark);
    text-decoration: underline;
  }

  .fcall {
    font-weight: 700;
    font-size: 1.08rem !important;
    color: var(--color-on-dark) !important;
  }

  .legal {
    border-top: 1px solid var(--line-dark);
    margin-top: 2.5rem;
    padding-top: 1.25rem;
  }

  .legal p {
    margin: 0;
    font-size: 0.85rem;
    color: var(--color-on-dark-soft);
  }
</style>
