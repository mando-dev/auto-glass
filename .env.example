# Copy to .env for local dev. Set the real value in the client's Vercel project
# under Settings > Environment Variables.
#
# Web3Forms access keys are public by design (they ship in the HTML). They are
# not a secret, but they do belong to the client's Web3Forms account.
PUBLIC_WEB3FORMS_KEY=
